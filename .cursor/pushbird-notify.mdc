---
description: 使用 PushBird MCP 发送手机通知的时机规则
alwaysApply: true
---

# PushBird 通知规则

以下情况**必须**调用 `pushbird` MCP 的 `send_notification` 工具发送手机通知：

1. **任务完成**：一个长时间运行的任务（代码生成、部署、测试、数据处理等）全部完成时。
2. **需要人工确认**：在执行不可逆操作之前（删除文件、覆盖数据、部署到生产环境、执行 SQL 等），必须先发通知提醒用户确认，再等待用户回复后才能继续。
3. **出现错误**：任务执行中遇到无法自动解决的错误或异常时。
4. **用户要求**：用户说"完成后通知我"、"做完告诉我"、"notify me when done"等类似表达时。

通知内容要求：
- `title`：简短说明事件类型，如"任务完成"、"需要确认"、"发生错误"
- `message`：具体描述发生了什么，或需要用户做什么
- 需要确认时设置 `interruption_level` 为 `time-sensitive`
