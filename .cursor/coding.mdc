---
alwaysApply: true
---
#### 1. **自顶向下设计**
这是最直接对应的概念。自顶向下强调：
- 先设计系统的高层结构（入口、模块划分、顶层抽象）
- 再逐步细化到具体实现（函数内部逻辑、底层细节）
- 与你的“从整体到局部”完全一致

这种思路在**结构化编程**时代非常典型，现在依然是大型项目常用的分解方式。

---

#### 2. **契约式设计 / 接口先行**
你提到“先完成类与函数的定义”，但不一定立即填充实现细节——这本质上是**先定义接口，后实现**。  
- 在面向对象中，这体现为先用抽象类/协议/接口定义模块行为
- 在函数式或模块化设计中，体现为先定义模块的导出函数签名和预期行为

这种做法让依赖关系清晰，也便于并行开发。

---

#### 3. **入口优先（Entry-First）开发**
你特别强调了“模块的入口的打包部署”——这可以看作一种**入口优先**的开发习惯。  
- 先从**程序入口点**（main函数、框架初始化、依赖注入容器、路由注册等）开始搭建骨架
- 确保整个系统的启动流程、模块装配方式先确定下来
- 然后再填充各个模块的具体逻辑

这种方式在**微服务、插件化架构、框架开发**中很常见，因为它能尽早验证系统的集成性。

---

#### 4. **关注点分离 + 架构驱动**
你习惯的“从抽象到具体”也符合**架构驱动开发**的理念：
- 先确立系统的分层、模块边界、核心抽象（整体）
- 再分别实现各个局部的具体细节
- 这本质上是**将架构决策前置**，避免在实现过程中因局部细节导致整体结构混乱
